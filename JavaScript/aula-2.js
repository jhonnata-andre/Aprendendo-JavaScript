
const n1 = 1;

let n2 = 2;

// let permite que você declare variáveis limitando seu escopo no bloco,
// instrução, ou em uma expressão na qual ela é usada.
// Isso é inverso da keyword var (en-US), que define uma variável
// globalmente ou no escopo inteiro de uma função,
// independentemente do escopo de bloco.

if (n1 === 1) (
    n2 = 4
)

// A condicional if é uma estrutura condicional que executa a afirmação,
// dentro do bloco, se determinada condição for verdadeira. Se for falsa,
// executa as afirmações dentro de else.

console.log(n2)